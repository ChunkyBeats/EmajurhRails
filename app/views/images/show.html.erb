<h3><%= @image.title %></h3>

<%= image_tag @image.attachment.url() %>

<% if current_user.id == @image.user_id  %>
  <p><%= link_to 'Delete', image_path(@image),
        :data => {:confirm => "Err yer shuuurrr?",
                  :method  => "delete"}%> |
  <%= link_to "Edit", edit_image_path %></p>
<% end %>
<%= link_to 'Back', root_path %>
